import { defineConfig } from 'astro/config';

export default defineConfig({
  // For GitHub Pages project URL:
  // https://TrollHeap.github.io/ck3-docs/
  // Keep base as '/ck3-docs/' for that repository.
  base: '/ck3-docs/',
  trailingSlash: 'always'
});
